{
  "name": "waterjet-simulator",
  "version": "1.0.0",
  "description": "Standalone waterjet cutting simulator",
  "private": true,
  "scripts": {
    "start": "serve -s build -l 3000",
    "build": "mkdir -p build && cp src/index.html build/index.html",
    "dev": "serve -s build -l 3000"
  },
  "dependencies": {},
  "devDependencies": {
    "serve": "^14.0.0"
  }
}